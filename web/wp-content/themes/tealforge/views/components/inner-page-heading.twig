<header class="tf-inner-page-heading" data-tf-reveal>
    {% if heading.icon %}
        <span class="tf-inner-page-heading__icon" aria-hidden="true">
            {% include 'components/icon.twig' with { name: heading.icon } only %}
        </span>
    {% endif %}

    {% if heading.eyebrow %}
        <p class="tf-inner-page-heading__eyebrow">{{ heading.eyebrow|e }}</p>
    {% endif %}

    <h1 class="tf-inner-page-heading__title">{{ heading.title|e }}</h1>

    {% if heading.text %}
        <p class="tf-inner-page-heading__text">{{ heading.text|e }}</p>
    {% endif %}
</header>
